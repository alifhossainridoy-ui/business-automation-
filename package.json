{
  "name": "rupzone-automation",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:dashboard": "pnpm --filter @rupzone/dashboard dev",
    "dev:backend": "pnpm --filter @rupzone/backend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
